{
  "tasks": {
    "dev": "deno run -A --watch --unstable index.ts",
    "start": "deno run -A --unstable index.ts",
    "proxyCheck": "deno run -A --unstable ./lib/proxyCheck.ts",
    "test": "deno test --unstable",
    "format": "deno fmt",
    "lint": "deno lint"
  },
  "imports": {
    "$/": "./lib/",
    "@/": "./",
    "~/": "https://deno.land/std@0.209.0/"
  }
}
